{% block sw_cms_block_preview_fh_tabs %}
    <div class="sw-cms-block-preview_fh-tabs">
        <div class="tabs">
            <div class="tab tab-active">{{ $tc('fh.blocks.tabs.preview.tab1') }}</div>
            <div class="tab">{{ $tc('fh.blocks.tabs.preview.tab2') }}</div>
            <div class="tab">{{ $tc('fh.blocks.tabs.preview.tab3') }}</div>
        </div>

        <div class="tab-container">{{ $tc('fh.blocks.tabs.preview.tabContainer') }}</div>
    </div>
{% endblock %}

