{% block sw_cms_element_preview_fh_tabs %}
    <div class="sw-cms-element-preview_fh-tabs">
        <div class="tabs">
            <div class="tab tab-active">{{ $tc('fh.elements.tabs.preview.tab1') }}</div>
            <div class="tab">{{ $tc('fh.elements.tabs.preview.tab2') }}</div>
        </div>

        <div class="tab-container">{{ $tc('fh.elements.tabs.preview.tabContainer') }}</div>
    </div>
{% endblock %}

